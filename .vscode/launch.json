{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start (Edge)",
            "request": "launch",
            "type": "msedge",
            "url": "http://localhost:5173/",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "npm: start"
        }
    ]
}
